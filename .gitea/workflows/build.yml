name: App CI Build

on:
  push:
    tags:
      - 'v*' 

permissions:
  contents: read
  actions: read

jobs:
  trigger-internal-build:
    uses: infras/workflow-templates/.gitea/workflows/docker-template.yml@64a262321661e8a94f14c40fc7ea7ea89544c05d
    with:
      image_name: 'simple-http-server'
      image_tag: ${{ gitea.ref_name }} 