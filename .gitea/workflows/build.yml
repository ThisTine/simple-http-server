name: App CI Build

on:
  push:
    tags:
      - 'v*' 

permissions:
  contents: read
  actions: read

jobs:
  trigger-internal-build:
    uses: infras/workflow-templates/.gitea/workflows/docker-template.yml@24202d7b006cfbcd755df3f97d6d4272ef75c4fe
    with:
      image_name: 'simple-http-server'
      image_tag: ${{ gitea.ref_name }} 